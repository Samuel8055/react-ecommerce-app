import React from "react";

const Title = ({ title }) => {
  return (
    <div className='text-center text-capitalize text-dark mt-4'>
      <h3>{title}</h3>
    </div>
  );
};

export default Title;
