import React from "react";
import Banner from "./Banner";

const Hero = () => {
  return (
    <section className='hero-container'>
      <Banner title='fashion collections' />
    </section>
  );
};

export default Hero;
